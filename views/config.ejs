<% config.css.forEach(function(obj, index){%>
    <link rel="stylesheet" href="<%= obj %>">
<% }) %>
<% config.js.forEach(function(obj, index){ %>
    <script type="text/javascript" src="<%= obj %>"></script>
<% }) %>